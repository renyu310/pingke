<%= link_to gravatar_for(current_user, 60), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "查看资料", current_user %></span>
<span><%= "#{current_user.microposts.count} 条状态" %></span>